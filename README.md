# STUDENTS-RESULTS-REGISTER

A project in c++ that demontrates the use of functons vectors stuctures and files
The project uses them to create a system that allows a user to
-View students grades
-Change them
-delete andmake all sorts of changes
It then finally uploads it to a file so the changes can be stored permanently and accessed again if manipulation is needed

